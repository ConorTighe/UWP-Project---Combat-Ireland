<Page xmlns:my="using:Microsoft.Toolkit.Uwp.UI.Controls" 
    x:Class="UWPCombatApp.Views.DisplayDrills"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPCombatApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <my:PullToRefreshListView x:ConnectionId='1'
            x:Name="ListView"
            MinWidth="200"
            Margin="24"
            HorizontalAlignment="Center"
            Background="White"
            OverscrollLimit="0.4"
            PullThreshold="100"
            IsPullToRefreshWithMouseEnabled="True">
            <my:PullToRefreshListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock AutomationProperties.Name="{Binding Title}"
                     Style="{StaticResource CaptionTextBlockStyle}"
                     Text="{Binding Title}"
                     TextWrapping="WrapWholeWords" />
                </DataTemplate>
            </my:PullToRefreshListView.ItemTemplate>
            <my:PullToRefreshListView.PullToRefreshContent>
                <TextBlock FontSize="16"
                   Opacity="0.5"
                   Text="Pull down to refresh data" />
            </my:PullToRefreshListView.PullToRefreshContent>
        </my:PullToRefreshListView>

        <my:HeaderedTextBlock x:ConnectionId='2' Name="testBox" Margin="100,100,0,0"/>
        <Button x:ConnectionId='3' Name="AddBtn" Height="30" Width="100" Content="Add new drill"                      />
        <Popup x:ConnectionId='4' x:Name="ppup" IsOpen="False" IsLightDismissEnabled="True"  
     Width="320" HorizontalAlignment="Left">
            <Popup.ChildTransitions>
                <TransitionCollection>
                    <!--<EdgeUIThemeTransition Edge="Left" />-->
                    <PaneThemeTransition Edge="Left" />
                </TransitionCollection>
            </Popup.ChildTransitions>
            <Grid Width="380" Height="{Binding ElementName=flyoutPane, Path=Height}"  Background="{ThemeResource FlyoutBackgroundThemeBrush}" >
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,10,10,10" >
                    <TextBlock x:ConnectionId='5' Name="NameText" Text="Enter Name:"/>
                    <TextBox x:ConnectionId='6'  Name="NameBox" Width="200" Height="50"/>
                    <TextBlock x:ConnectionId='7' Name="SetsText" Text="Enter Sets:"/>
                    <TextBox x:ConnectionId='8'  Name="SetsBox" Width="200" Height="50"/>
                    <TextBlock x:ConnectionId='9' Name="TimeText" Text="Enter Time to complete:"/>
                    <TextBox x:ConnectionId='10' Name="TimeBox" Width="200" Height="50"/>
                    <Button x:ConnectionId='11' Name="SubmitBtn" Height="30" Width="100" Content="Submit"                        />
                </StackPanel>
            </Grid>
        </Popup>
    </Grid>
</Page>

